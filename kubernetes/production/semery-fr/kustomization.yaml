apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: semery-fr
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
secretGenerator:
  - name: semery-fr-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
