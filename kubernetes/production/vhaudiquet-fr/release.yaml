apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vhaudiquet-fr
  namespace: vhaudiquet-fr
spec:
  interval: 1m
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        name: vhaudiquet-fr
        namespace: vhaudiquet-fr
      chart: vhaudiquet-fr
      version: '>=0.1.0'
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: vhaudiquet-fr-values
