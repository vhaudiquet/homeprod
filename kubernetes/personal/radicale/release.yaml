apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radicale
  namespace: radicale
spec:
  interval: 1m
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        name: derwitt
        namespace: radicale
      chart: radicale
      version: "2.1.0"
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: radicale-values
