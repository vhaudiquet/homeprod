apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: renovate
  namespace: renovate
spec:
  interval: 1m
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        name: renovate
        namespace: renovate
      chart: renovate
      version: "46.260.4"
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: renovate-values
